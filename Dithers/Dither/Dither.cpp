#include "Dither.h"

Dither::Dither(const std::size_t& matrixRows, const std::size_t& matrixCols) :
    matrixRows(matrixRows), matrixCols(matrixCols) {
        ;
}

void Dither::setUpDither(Image* image) {
    ;
}

void Dither::ditherImage(Image* image) {
    ;
}